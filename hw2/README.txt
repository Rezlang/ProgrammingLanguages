Group Members: Josh Moskoff,   Nick Maniscalco
               moskoj@rpi.edu, manisn2@rpi.edu

Known Features:
    - Create a user-specified number of file servers
    - Replicates stored files up to a user-specified limit
    - Store and Retrieve Files (via create(), get())
    - Simulates server crashes (via deactivate())
    - Terminates gracefully (via quit())
    - Supports file servers across distributed systems



Known Bugs:
    none :)

